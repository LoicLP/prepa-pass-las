import ResetPasswordClient from './ResetPasswordClient';

export const metadata = {
  title: 'Nouveau mot de passe - Prépa PASS/LAS',
  robots: { index: false },
};

export default function Page() {
  return <ResetPasswordClient />;
}
